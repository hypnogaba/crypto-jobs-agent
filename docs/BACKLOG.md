# Відкладене

Одне речення на правку. Не плани й не ідеї — лише те, що ми **знайшли, зрозуміли
й свідомо не зробили зараз**. Кожен рядок мусить бути таким, щоб через місяць
його можна було взяти в роботу без розслідування наново.

Чого тут не має бути: продуктових питань (вони в `ROADMAP.md`, розділ 9),
здогадів і побажань. Якщо причина невідома — це не рядок сюди, а задача
розібратись.

Формат: `— що зробити (чому це важливо) · де`
Зроблене видаляємо, а не викреслюємо: історія лишається в git.

---

## Джерела

— Повернути в репозиторій юніти, які живуть лише на сервері: 31.08
  `systemctl list-timers` показав увімкненими `nextrole-review.timer` (Нд
  06:00, тижневий самоперегляд) і `nextrole-discover.timer` (Нд 04:00), а
  їхніх файлів у `scanner/deploy/` немає — переставивши сканер за README,
  обидва втратимо мовчки · `scanner/deploy/`

— Дописати `nextrole-twitter.timer` у рядок `systemctl enable --now`
  інструкції (там перелічено лише scan/watchdog/digest/requests, тож
  щотижневу розвідку розгортання за README не вмикає) ·
  `scanner/deploy/README.md:31`


## Підбір

Жива скарга 31.08: людина написала, що шукає вакансію вхідного рівня, і
отримала middle та senior. Причина не в тому, що ми не почули — слова про
рівень розбір КЛАДЕ в `customRole` і `wishes`, і вони доїжджають до підбору.
Їх викидають уже під час підрахунку, двома стоп-листами. Прибрання питання про
рівень (`3a9a88e`) обіцяло, що слова замінять кнопку; ось чому не замінили.

— Вийняти «junior/middle/senior/entry» зі стоп-листів або дати їм власне
  правило (зараз `GENERIC_ROLE_WORDS` і `GENERIC_WISH_WORDS` викреслюють ці
  слова ще до пошуку, тож «хочу junior позицію» дає нуль слів і +0 балів —
  обіцянка коміту «у побажаннях вони справді шукаються» не виконується) ·
  `scanner/src/match.ts:196` і `:242`

— Полагодити гірше за нуль: фраза про junior САМА пропускає senior-вакансію
  (`meaningfulRoleWords` знімає «junior», `partiallyMatchesRole` працює через
  `.some()`, тож «Senior Product Manager» збігається з «junior product
  manager», дає `rolePart +5` і факт `role`, а `onTopic` пускає вакансію
  далі саме за цим фактом) · `scanner/src/match.ts:211`, `:214`, `:555`

— Побажання не мають від'ємного плеча: вакансія, що суперечить написаному,
  не карається взагалі, лише збіг додає (`WISH_WORD_BONUS`) — тому «не хочу
  senior» і «хочу senior» важать однаково · `scanner/src/match.ts:264`

— Звести `STRONG_SCORE` з дійсністю: у ньому досі 21 = сфера 6 + роль 12 +
  **рівень 3**, якого вже немає, тож `matchPercent` занижує кожен відсоток
  приблизно на 14% · `scanner/src/match.ts:605`

— Прибрати мертвий підпис `fact.level` («твій рівень») — факт `k: "level"`
  ніде не створюється · `web/src/lib/i18n.ts:264` і `:515`

— Не слати добірку в порожню анкету: людина з нулем сфер отримала 5 листів
  (див. рядок у «Бот») — підбору не було з чого робитись, а мовчазна відмова
  сама себе не пояснює · `scanner/src/digest.ts`

— Узгодити токенізацію: `roleWords` ділить по дефісу, `wishWords` — ні, тому
  «entry-level» у побажаннях лишається одним токеном і шукається дослівно, а в
  назві ролі розпадається на два · `scanner/src/match.ts:179` проти `:252`

ВІДКИНУТО (перевірено, вигоди немає): робити зарплату й локацію жорсткими
межами замість доданків. Паралельна сесія поміряла на всіх шести живих
профілях: у топ-5 офісних вакансій нуль при `remote_only`, а варіант із межею
дав байт у байт той самий результат. Штраф −6 за onsite тримає й без межі.

## Бот

— Нагадати заповнити анкету тому, хто прив'язав Telegram, але лишився з
  порожньою анкетою (жива людина від 28.08: нуль сфер, а добірок уже 5, і
  відповіла вона на них «не те» — тобто ми шлемо їй те, що не могли підібрати,
  замість одного питання) · `web/src/lib/bot.ts`, ознака — `profiles.spheres`
  порожній при `users.telegram_chat_id IS NOT NULL`


## Кабінет

## Панель

— «Люди»: показувати 10 рядків і сторінкувати з номерами (запит уже стоїть
  `LIMIT 50`, і всі 50 малюються одразу; при сотні людей блок перестане
  поміщатися на екран) · `web/src/app/(app)/admin/page.tsx:657` і таблиця на 807

— «Люди»: фільтр по щаблю лійки (зараз єдиний порядок — за датою приходу, тож
  «хто застряг без Telegram» доводиться шукати очима по всьому списку, хоча
  лійка поруч уже рахує саме ці групи) · там само, 794–798

— «Як ми ростемо»: дотик по стовпчику розкриває графік на повний (зараз
  `Spark` показує підказку лише при наведенні — на телефоні наведення немає, і
  числа за день не дістати взагалі) · `web/src/app/(app)/admin/page.tsx:226`

— «Джерела → Поіменно»: згорнути обидві таблиці в `<details>` (44 рядки
  «Загальні — бачать усі» щодня займають екран, а розгортають їх лише коли
  щось зламалось; свій прийом уже є поруч — рядки 986, 1003, 1185) ·
  `web/src/app/(app)/admin/page.tsx:1158`

— «Дошки»: розділити таблицю на «весь світ» і «країни» або згорнути її —
  це два різні питання, і разом вони не читаються (ознака вже є:
  `g.country === "*"` проти коду країни) · `web/src/app/(app)/admin/page.tsx:1340`

— «Самонавчання»: окремий блок — з чого воно складається, коли наступний
  прогін і як пройшов останній (зараз панель читає лише ОДИН рядок
  `scan_runs ORDER BY started_at DESC LIMIT 1`, тобто «як пройшов скан», і
  жодного слова про тижневий самоперегляд, розвідку чи ваги `user_tuning`;
  розкладу в базі немає взагалі — він живе в systemd на VPS, тож «наступний»
  доведеться або взяти з `scanner/deploy/*.timer`, або писати в базу при
  кожному прогоні) · `web/src/app/(app)/admin/page.tsx:391`

— «Посилання, які не прийнялись»: блок вимагає дії, але не пропонує жодної, і
  половина рядків узагалі не потребує людини. Перевірено в базі 31.08: один
  прогін записав КОЖНУ адресу по 4–5 разів за 20 секунд (10:25:49 → 10:26:13);
  `beincrypto.com` і `kyzzen.io` уже лежать нижче в «Відкинуто після виміру» як
  назавжди неможливі — а кнопка пропонує «спробувати ще»; `jobstash.xyz`,
  `web3.career`, `crypto-careers.com` і `cryptocurrencyjobs.co` мають вердикт
  «не розпізнано», хоч усі чотири вже підключені й ПРАЦЮЮТЬ (1638, 438, 20 і
  85 вакансій). Тобто власника просять розібратися з тим, що вже зроблено або
  вже відкинуто. Треба, щоб система знімала такі рядки сама: дедуплікація за
  адресою, звірка з наявними джерелами перед вердиктом «не розпізнано», і
  вічний список знайомих безнадійних · таблиця `source_intake`,
  `web/src/app/(app)/admin/page.tsx:1185`

— Лійка й таблиця рахують «анкету» по-різному й суперечать одна одній на
  одному екрані: лійка бере `COUNT(*) FROM profiles` («12 · 100%»), а
  стовпчик у таблиці — `sphereCount(spheres) > 0`, і поруч стоїть троє з
  позначкою «немає» · `web/src/app/(app)/admin/page.tsx:636` проти `:815`


---

## Відкинуто після виміру

Рядки нижче лишаються навмисно: без них наступний витратить вечір на ту саму
спробу. Кожен має причину, а не враження.

**cryptojobslist.com і beincrypto.com.** Увесь домен за інтерактивним викликом
Cloudflare (`cf-mitigated: challenge`), усі 10 типових шляхів RSS дають 403,
піддоменів `feeds./rss./static.` не існує. Обійти це кодом не можна; писати їм
власник не хоче. Закрито.

**Headless-браузер заради kyzzen.io та ethereumjobboard.com.** Поміряв здобич
перед тим, як платити: у sitemap ethereumjobboard рівно **дев'ять** сторінок
вакансій, і кожна порожня без JavaScript (ні schema.org, ні og:title, ні h1).
У kyzzen sitemap на 508 посилань, з них 413 — `/collections`: це не дошка
вакансій, `/jobs` там збоку. Дев'ять вакансій не варті Chrome на 400 МБ на
сервері, який уже раз падав через межу пам'яті. Проти Cloudflare-виклику він
усе одно не допоміг би — той розпізнає headless окремо.

**Графік відвідувань сайту в панелі.** Три шляхи, усі гірші за наявний:
GraphQL Cloudflare потребує окремого токена; лічильник у D1 коштував би запис
на КОЖНЕ відкриття сторінки, а D1 і так на межі; Analytics Engine — ще одна
прив'язка. При цьому відповідь уже існує: Cloudflare Web Analytics підключено,
і його власна панель показує це краще, ніж показали б ми. Дублювати чуже
робоче за ціною нашого вузького місця не варто.

**Розрізняти «віддалено» серед решти 2 900.** Пряму суперечність закрито
(«Tallinn Office» скасовує прапорець, 79 рядків). Далі перевірено ТРИ підходи,
усі відкинуто на живих даних:
1. *вигляд локації* — на 22 випадкових зразках помилявся на кожній четвертій:
   «Toronto - Canada» і «Sydney Office» ставали віддаленими, «England» ні;
2. *одностайність компанії* («всі вакансії фірми віддалені») — відкинула б
   Docker і n8n, які справді віддалені й теж пишуть місто;
3. *назва посади* — під фізичні ролі підпадають 33 рядки з 2 900, і серед них
   «Global Supply Manager — Contract Manufacturing» у «Remote-Friendly, United
   States» (справді віддалена, manufacturing — галузь) та «Professional
   Learning Facilitator», упійманий за підрядком «Facilit».

Решта 2 900 — здебільшого чесне «віддалено в межах регіону». Помилково сховати
віддалену вакансію гірше, ніж показати зайву офісну, тож четвертого здогаду не
робимо. Зрушити це може лише сигнал, якого в нас немає: текст самого
оголошення або відповідь роботодавця.

**Getro: нові колекції лишаються вимкненими.** Розвідка щонеділі знаходить
живі колекції по всьому діапазону й записує їх `enabled=0`. Вмикати їх гуртом
не будемо: 20 колекцій у щоденному скані коштують 2.5–5 хвилин, тож кілька
сотень зробили б скан півторагодинним, а D1 і без того на межі. Цінність
колекції не в щоденному читанні: 80% її посилань ведуть просто в ATS
роботодавця, і ці компанії після збору обходить R1 щодня без Getro. Тобто
розвідка забирає головне — компанії — навіть із вимкненої колекції.
