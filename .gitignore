.superpowers/

# Local-only notes containing personal data — never commit
*.private.md

# scanner
# Без скісної риски наприкінці: у робочих гілках node_modules буває
# СИМЛІНКОМ на головний каталог, а взірець із рискою symlink не ловить —
# і він тричі потрапляв у коміт.
scanner/node_modules
scanner/dist/
scanner/.env

# Робочі каталоги агента — не частина продукту
.claude/

# Локальний кеш wrangler (id акаунта) — не частина продукту
/.wrangler/
